    accessLevel: expect.any(String),
  expect(room.accessLevel).toMatch(/PUBLIC|PRIVATE|PROTECTED/);
    accessLevel: expect.any(String),
  expect(room.accessLevel).toMatch(/PUBLIC|PRIVATE|PROTECTED/);