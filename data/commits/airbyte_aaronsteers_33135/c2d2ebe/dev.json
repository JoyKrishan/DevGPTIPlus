  "description": "Contains detailed information about each GitHub repository, including metadata, ownership, and configuration details.",
  "entity_type": "repository",
  "foreign_keys": {
    "owner_id": "Links to the 'id' in the Users stream (Owner of the repository)."
  },
      "type": ["null", "integer"],
      "description": "The unique identifier for the repository."
      "type": ["null", "string"],
      "description": "A node-specific identifier for the repository."
      "type": ["null", "string"],
      "description": "The name of the repository."
      "type": ["null", "string"],
      "description": "The full name of the repository including the owner's name."
      "$ref": "user.json",
      "description": "An object containing information about the repository owner."
      "type": ["null", "boolean"],
      "description": "Indicates whether the repository is private."
      "type": ["null", "string"],
      "description": "The URL of the repository on GitHub's web interface."
      "type": ["null", "string"],
      "description": "A brief description of the repository's purpose and contents."
      "type": ["null", "boolean"],
      "description": "Indicates whether the repository is a fork of another repository."
      "type": ["null", "string"],
      "description": "The API URL of the repository."
      "type": ["null", "string"],
      "description": "The URL to download a tar archive of the repository."
      "type": ["null", "string"],
      "description": "The URL listing the assignees for issues in the repository."
      "type": ["null", "string"],
      "description": "The URL for blobs (binary large objects) in the repository."
      "type": ["null", "string"],
      "description": "The URL listing the branches in the repository."
      "type": ["null", "string"],
      "description": "The URL listing the collaborators of the repository."
      "type": ["null", "string"],
      "description": "The URL for comments in the repository's issues and pull requests."
      "type": ["null", "string"],
      "description": "The URL listing the commits in the repository."
      "type": ["null", "string"],
      "description": "The URL to compare two commits in the repository."
      "type": ["null", "string"],
      "description": "The URL to retrieve the contents of the repository."
      "type": ["null", "string"],
      "description": "The URL listing the contributors to the repository."
      "type": ["null", "string"],
      "description": "The URL listing the deployments of the repository."
      "type": ["null", "string"],
      "description": "The URL for downloading resources from the repository."
      "type": ["null", "string"],
      "description": "The URL listing the events in the repository."
      "type": ["null", "string"],
      "description": "The URL listing the forks of the repository."
      "type": ["null", "string"],
      "description": "The URL listing the Git commits in the repository."
      "type": ["null", "string"],
      "description": "The URL listing the Git references (branches, tags, etc.) in the repository."
      "type": ["null", "string"],
      "description": "The URL listing the Git tags in the repository."
      "type": ["null", "string"],
      "description": "The Git URL to clone the repository."
      "type": ["null", "string"],
      "description": "The URL for the issue comments in the repository."
      "type": ["null", "string"],
      "description": "The URL listing the issue events in the repository."
      "type": ["null", "string"],
      "description": "The URL listing the issues in the repository."
      "type": ["null", "string"],
      "description": "The URL listing the deploy keys of the repository."
      "type": ["null", "string"],
      "description": "The URL listing the labels for issues in the repository."
      "type": ["null", "string"],
      "description": "The URL listing the programming languages used in the repository."
      "type": ["null", "string"],
      "description": "The URL listing the merges in the repository."
      "type": ["null", "string"],
      "description": "The URL listing the milestones in the repository."
      "type": ["null", "string"],
      "description": "The URL for notifications related to the repository."
      "type": ["null", "string"],
      "description": "The URL listing the pull requests in the repository."
      "type": ["null", "string"],
      "description": "The URL listing the releases in the repository."
      "type": ["null", "string"],
      "description": "The SSH URL to clone the repository."
      "type": ["null", "string"],
      "description": "The URL listing the stargazers of the repository."
      "type": ["null", "string"],
      "description": "The URL listing the commit statuses in the repository."
      "type": ["null", "string"],
      "description": "The URL listing the subscribers to the repository."
      "type": ["null", "string"],
      "description": "The URL for managing the subscription to the repository."
      "type": ["null", "string"],
      "description": "The URL listing the tags in the repository."
      "type": ["null", "string"],
      "description": "The URL listing the teams associated with the repository."
      "type": ["null", "string"],
      "description": "The URL listing the Git trees in the repository."
      "type": ["null", "string"],
      "description": "The URL to clone the repository via HTTPS."
      "type": ["null", "string"],
      "description": "The URL of the repository's mirror."
      "type": ["null", "string"],
      "description": "The URL listing the webhooks in the repository."
      "type": ["null", "string"],
      "description": "The SVN URL to checkout the repository."
      "type": ["null", "string"],
      "description": "The URL of the repository's homepage."
      "type": ["null", "string"],
      "description": "The primary programming language used in the repository."
      "type": ["null", "integer"],
      "description": "The number of times this repository has been forked."
      "type": ["null", "integer"],
      "description": "The number of stars this repository has received."
      "type": ["null", "integer"],
      "description": "The number of users watching the repository."
      "type": ["null", "integer"],
      "description": "The size of the repository in kilobytes."
      "type": ["null", "string"],
      "description": "The default branch of the repository."